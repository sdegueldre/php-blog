{% extends 'common.twig' %}

{% block content %}
<!-- Page Content -->
<div class="container">
  <div class="row">
    <!-- Blog Entries Column -->
    <div class="col-lg-8">

      <h1 class="my-4">Articles written by {{ author }}</h1>

      {% for article in articles %}
      {{ include("articleCard.twig") }}
      {% endfor %}

    </div>

    {{ include('sidebar.twig') }}

  </div>
  <!-- /.row -->

</div>
<!-- /.container -->
<div class="flex-grow-1"></div>
{% endblock %}
